# --- API layer ---
#fastapi==0.115.0
#uvicorn[standard]==0.30.6
#pydantic==2.9.2
python-dotenv==1.0.1

pydantic>=2.11.7
fastapi>=0.116.1
uvicorn>=0.35.0
# --- Scraping ---
requests==2.32.3
# playwright==1.47.0   # no longer needed -- catalog is fetched via direct JSON feed instead

# --- Data processing ---
pandas==2.2.3

# --- Retrieval ---
sentence-transformers==3.1.1
rank-bm25==0.2.2
numpy==1.26.4

# --- Cross-encoder reranking (conditional use) ---
# sentence-transformers already provides CrossEncoder class

# --- LLM ---
groq==0.11.0
# google-generativeai==0.8.3   # uncomment if switching to Gemini

# --- Orchestration (thin routing wrapper only, no persistence features used) ---
langgraph==0.2.34

# --- Testing / evaluation ---
pytest==8.3.3
httpx==0.27.2
requests

sentence-transformers